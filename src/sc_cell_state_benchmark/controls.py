# This module is intentionally empty.
#
# random_gene_set was superseded by matched_random_gene_sets in scoring.py,
# which uses a seeded RNG, respects adata.raw, and returns multiple sets.
#
# permute_labels was never wired into the benchmark pipeline.
# A permuted-label null is noted in docs/NEXT_STEPS.md as a v1.1 addition.
